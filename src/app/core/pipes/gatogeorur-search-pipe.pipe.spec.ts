import { GatogeorurSearchPipePipe } from './gatogeorur-search-pipe.pipe';

describe('GatogeorurSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GatogeorurSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
