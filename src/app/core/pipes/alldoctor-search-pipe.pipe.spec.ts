import { AlldoctorSearchPipePipe } from './alldoctor-search-pipe.pipe';

describe('AlldoctorSearchPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AlldoctorSearchPipePipe();
    expect(pipe).toBeTruthy();
  });
});
